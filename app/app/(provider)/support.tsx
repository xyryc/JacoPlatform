import { SupportScreen } from "@/src/components/SupportScreen";

export default function SupportPage() {
  return (
    <SupportScreen
      title="Help & Support"
      heroTitle="How can we help?"
      heroBody="Reach out about orders, payments, account issues, or anything else that needs a closer look."
      heroIcon="help-buoy"
    />
  );
}
